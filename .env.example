# Create a Resend API key at https://resend.com/api-keys and add these values
# in your Vercel project settings (or a local .env.local file for development).
RESEND_API_KEY=re_your_api_key
# Must be an address on a domain verified in Resend, for example:
CONTACT_FROM_EMAIL="Portfolio <contact@yourdomain.com>"
